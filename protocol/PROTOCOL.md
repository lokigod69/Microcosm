# Protocol — MICROCOSM
Mode: 1 Autopilot  (set 2026-07-10 — human explicitly out of the loop until v1 ships)
Brain: memory/ — coordination here, memory there
Definition of v1-done: `npm test` green including same-seed determinism; `npm run dev`
serves a world where — observably in the browser — tribes form, coin words, generate at
least one god and one myth within ~20 in-world years; the Chronicle renders both voices;
all 3 themes cycle live; `npm run build` produces a working static bundle; repo pushed
to github.com/lokigod69/MICROCOSM.
Decision rights: mode-standard (agent decides everything except spending money or
touching other projects; outward actions limited to the lokigod69 GitHub push).
Human's standing duties: paste NEXT_STEP on context resets; play with the built world
and report taste feedback afterwards.
Workstreams: main
Custom rules for this project: determinism invariant (AGENTS.md) outranks convenience;
in-world names never hardcoded English.
